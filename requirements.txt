# Updated 2026-05-04: patch-level security updates
# Backend
fastapi>=0.118,<1.0  # bumped: starlette>=0.49.1 security floor needs newer fastapi
starlette>=0.49.1,<1.0.0  # fastapi ecosystem caps <1.0; 8 findings needing starlette 1.x WAIVED (see receipt)  # 0.49.1 clears 9 pip-audit findings
uvicorn[standard]==0.34.0  # was 0.27.0, updated for security fixes
sqlalchemy==2.0.36  # was 2.0.25, updated for bug fixes
psycopg2-binary==2.9.10  # 2.9.9 lacks cp313 wheels
pydantic==2.10.4  # was 2.5.3, updated for security fixes
pydantic-settings==2.7.1  # was 2.1.0, updated for compatibility

# Data fetching
httpx>=0.26.0,<0.28.0
feedparser>=6.0.0
alembic>=1.13.0  # DB migrations — required by deploy and dev workflows
aiohttp==3.14.3  # 3.11.11 carried 34 pip-audit findings

# Polymarket integration
py-clob-client-v2>=1.0.0
py-order-utils>=0.3.0
py-builder-signing-sdk>=0.0.2
py-builder-relayer-client>=0.0.1

# Kalshi API auth (RSA-PSS signatures)
cryptography>=42.0.0

# Weather data (using Open-Meteo API)
numpy==2.4.6  # py3.14-compatible (1.26.x has no cp314 wheels)
nest_asyncio==1.6.0  # required for sync balance fetch in async orchestrator
pandas==3.0.3  # py3.14-compatible (2.2.x has no cp314 wheels)
scipy==1.16.1  # was 1.14.1, bumped to support numpy>=2.3 (1.14.1 caps numpy<2.3)

# Scheduling
apscheduler==3.10.4

# AI / LLM
anthropic>=0.40.0
groq>=0.4.0
openai>=1.12.0

# Logging
loguru>=0.7.0
structlog>=24.1.0

# Utils
python-dotenv==1.2.2  # clears PYSEC finding
orjson==3.11.6  # clears 2 PYSEC findings
psutil>=5.9.0
# NOTE: urllib3 intentionally unpinned — runtime is 2.7.0 (findings cleared)
# but lighter-sdk 1.0.9 declares urllib3<2.1.0, so any floor here is
# unresolvable. Waived in docs/plans/pip-audit-receipt.txt.

# Polymarket auth & execution
eth-account>=0.11.0
websockets>=12.0

# Telegram bot
python-telegram-bot>=21.0

# Testing
pytest>=8.0,<9  # PYSEC-2026-1845 (dev tool) WAIVED: pytest 9 major jump deferred
pytest-asyncio>=0.23
pytest-httpx>=0.30
respx>=0.21

# Phase 2: Redis cache and queue
redis>=5.0.0
arq>=0.25.0

# Rate limiting
slowapi>=0.1.9

# Circuit breakers
pybreaker>=1.0.0

# Prometheus metrics
prometheus-client>=0.20.0

# Evolutionary algorithms (NSGA-II multi-objective optimization)
deap>=1.4.0

# HuggingFace dataset integration
huggingface_hub>=0.20.0
pyarrow>=14.0.0
vectorbt>=0.26.0

# ML model serialization (safe alternative to pickle — standard for sklearn)
joblib>=1.3.0

# Prediction market integrations (Phase 1 — improvement-summary.md)
# pmxt>=0.1.0  # not on PyPI, local import only
# backtesting.py removed - local module at backend/backtesting/
# polymarket-paper-trader>=0.1.0  # not on PyPI
# polymarket-sdk>=0.1.0  # not on PyPI
# polymarket-mcp>=0.1.0  # not on PyPI

# Dev
ruff>=0.3
cachetools>=5.3.3
scikit-learn>=1.3.2

# DEX Providers
hyperliquid-python-sdk>=0.23.0
ostium-python-sdk>=3.2.0
lighter-sdk==1.0.9  # pinned: resolution-too-deep guard
ccxt>=4.5.0
